# XCStrings Manager

一个用于管理 Xcode `.xcstrings` 本地化文件的 VS Code 扩展。

## 功能特性

### 🔀 拆分 XCStrings 文件

将 `.xcstrings` 文件按语言拆分成多个 JSON 文件，方便翻译和版本控制。

**使用方法：**
1. 在资源管理器中右键点击 `.xcstrings` 文件
2. 选择 "XCStrings: Split File"
3. 输入输出目录路径（默认为 `./locales`）
4. 等待拆分完成

**或者：**
- 打开 `.xcstrings` 文件
- 按 `Cmd+Shift+P` (macOS) 或 `Ctrl+Shift+P` (Windows/Linux)
- 输入 "XCStrings: Split File"

### 🔗 合并 JSON 文件

将多个语言的 JSON 文件合并回 `.xcstrings` 格式。

**使用方法：**
1. 在资源管理器中右键点击包含语言 JSON 文件的目录
2. 选择 "XCStrings: Merge Files"
3. 输入源语言代码（默认为 `en`）
4. 等待合并完成

**或者：**
- 按 `Cmd+Shift+P` (macOS) 或 `Ctrl+Shift+P` (Windows/Linux)
- 输入 "XCStrings: Merge Files"
- 选择包含 JSON 文件的目录

### 📊 对比 XCStrings 文件

深度对比两个 `.xcstrings` 文件，验证它们是否一致。

**使用方法：**
1. 按 `Cmd+Shift+P` (macOS) 或 `Ctrl+Shift+P` (Windows/Linux)
2. 输入 "XCStrings: Compare Files"
3. 选择第一个文件
4. 选择第二个文件
5. 查看对比报告

## 工作流程示例

### 典型的本地化工作流程

```
1. 拆分原始文件
   Localizable.xcstrings → en.json, zh-Hans.json, zh-Hant.json, ...

2. 编辑各个语言文件
   使用你喜欢的编辑器或翻译工具

3. 合并回 xcstrings 格式
   en.json, zh-Hans.json, ... → Localizable.xcstrings

4. 验证合并结果
   对比原始文件和生成文件
```

## 保留的数据

扩展会完整保留以下所有数据：

- ✅ `sourceLanguage` - 源语言
- ✅ `version` - 版本号
- ✅ `stringUnit` - 翻译单元
- ✅ `state` - 翻译状态
- ✅ `value` - 翻译值
- ✅ `variations` - 复数形式
- ✅ `comment` - 注释
- ✅ `shouldTranslate` - 是否需要翻译标记
- ✅ 所有其他自定义字段

## 输出格式

### 拆分后的 JSON 文件格式

```json
{
  "All Items": {
    "stringUnit": {
      "state": "translated",
      "value": "所有单品"
    }
  },
  "%lld items": {
    "variations": {
      "plural": {
        "other": {
          "stringUnit": {
            "state": "translated",
            "value": "%lld件单品"
          }
        }
      }
    }
  },
  "_root_metadata": {
    "sourceLanguage": "en",
    "version": "1.0"
  }
}
```

## 安装

### 从源码安装

1. 克隆或下载此项目
2. 在项目目录中运行：
   ```bash
   npm install
   npm run compile
   ```
3. 按 `F5` 在扩展开发主机中运行

### 打包安装

```bash
npm install -g vsce
vsce package
code --install-extension xcstrings-manager-1.0.0.vsix
```

## 要求

- VS Code 1.80.0 或更高版本
- Node.js 18.0 或更高版本

## 已知问题

目前没有已知问题。如果你发现任何问题，请在 GitHub 上提交 issue。

## 发布说明

### 1.0.0

初始版本发布

- 支持拆分 `.xcstrings` 文件
- 支持合并 JSON 文件
- 支持对比 `.xcstrings` 文件
- 完整保留所有元数据和特殊字段

## 许可证

MIT

---

**享受使用！** 🎉

